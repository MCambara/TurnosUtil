package pruebas;

import umg.edu.gt.prograIII.list.ListaEnlazada;
import umg.edu.gt.prograIII.tree.ArbolBinario;

public class Main {
    /*public static void main(String[] args) {
        // Crear personas
        Persona p1 = new Persona(25, "Ana", "Perez", 123456);
        Persona p2 = new Persona(30, "Luis", "Gomez", 654321);
        Persona p3 = new Persona(20, "Maria", "Lopez", 789012);

        // --- LISTA ---
        System.out.println("=== LISTA ENLAZADA ===");
        ListaEnlazada<Persona> lista = new ListaEnlazada<>();

        // Insertar 3
        lista.agregar(p1);
        lista.agregar(p2);
        lista.agregar(p3);

        System.out.println("Lista después de insertar 3 personas:");
        lista.mostrar();

        // Eliminar 1
        lista.eliminar(p2);
        System.out.println("Lista después de eliminar a Luis Gomez:");
        lista.mostrar();

        // Insertar duplicado (p3 otra vez)
        lista.agregar(p3);
        System.out.println("Lista después de insertar duplicado (Maria Lopez):");
        lista.mostrar();

        // --- ARBOL ---
        System.out.println("\n=== ÁRBOL BINARIO ===");
        ArbolBinario<Persona> arbol = new ArbolBinario<>();

        // Insertar 3
        arbol.insertar(p1);
        arbol.insertar(p2);
        arbol.insertar(p3);

        System.out.println("Árbol InOrden después de insertar 3 personas:");
        arbol.inOrden();

        // Eliminar 1 (por edad)
        arbol.eliminar(p2);

        System.out.println("Árbol InOrden después de eliminar a Luis Gomez:");
        arbol.inOrden();

        // Insertar duplicado (p3 otra vez)
        arbol.insertar(p3);  // Como modificamos el árbol para ignorar duplicados, no debería agregarlo

        System.out.println("Árbol InOrden después de intentar insertar duplicado (Maria Lopez):");
        arbol.inOrden();
    }*/
}
